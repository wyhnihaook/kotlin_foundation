<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/camera_ui_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">




        <ImageView
            android:layout_marginLeft="@dimen/dp_15"
            android:id="@+id/photo_view"
            android:layout_width="@dimen/dp_40"
            android:layout_height="@dimen/dp_40"
            android:scaleType="fitXY"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="@+id/camera_capture_view"
            app:layout_constraintBottom_toBottomOf="@+id/camera_capture_view"/>


        <ImageView
            android:id="@+id/camera_capture_view"
            android:layout_width="@dimen/dp_60"
            android:layout_height="@dimen/dp_60"
            android:layout_margin="@dimen/dp_15"
            android:src="@drawable/ic_camera_button"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent" />


        <ImageView
            android:id="@+id/camera_switch_view"
            android:layout_width="@dimen/dp_40"
            android:layout_height="@dimen/dp_40"
            android:layout_marginRight="@dimen/dp_15"
            android:src="@drawable/ic_camera_switch"
            app:layout_constraintBottom_toBottomOf="@+id/camera_capture_view"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="@+id/camera_capture_view" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
